{
  "default": {
    "require": [
      "src/test/stepDefinitions/*.ts",
      "src/test/hooks/*.ts"
    ],
    "paths": ["src/test/features/*.feature"],
    "requireModule": ["ts-node/register"],
    "format": ["progress"],
    "publishQuiet": true
  }
}
